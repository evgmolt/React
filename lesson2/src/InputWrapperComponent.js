import './App.css';

const InputWrapperComponent = (props) => {
    return (
        <div className="inputWrapper">
            props.input;
            props.button;
        </div>
    )
}

export default InputWrapperComponent;